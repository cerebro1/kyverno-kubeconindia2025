apiVersion: v1
kind: Pod
metadata:
  annotations: 
     app: nginx
  name: patched
  namespace: test
spec:
      containers:
      - name: nginx
        image: nginx
---
# apiVersion: v1
# kind: Pod
# metadata:
#   annotations: 
#      app: nginx
#   name: non-patched
#   namespace: default
# spec:
#       containers:
#       - name: nginx
#         image: nginx